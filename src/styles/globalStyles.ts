import * as colors from './colors';

// Card Styles
export const cardHeight = 120;
export const cardWidth = cardHeight * 0.67;
export const cardBorder = `2px solid ${colors.cardBorderColor}`;
export const cardFontSize = cardHeight / 5;
